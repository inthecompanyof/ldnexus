%h1.logo.row
  %i.glyphicon.glyphicon-fire
  Help!
.selection_buttons
  = link_to 'I need help', topics_path, class: 'btn'
.row
  .col-md-6
    %h2 Pending help requests:
    %ul.supports
      - waiting_for_help.each do |not_done|
        %li= render 'supports/not_done', not_done: not_done
  .col-md-6
    %h2 Recently helped:
    %ul.supports
      - recent_supports.each do |support|
        %li= render support

  .col-md-12.text-center
    = link_to users_path, class: 'btn btn-primary' do
      %i.glyphicon.glyphicon-user
      See users ranking

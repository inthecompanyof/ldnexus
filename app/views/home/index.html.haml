%section.intro
  .container
    .row
      .col-lg-12
        %h1 Help!
    .row
      .col-lg-4.col-lg-offset-4
        = link_to 'I need help', topics_path, class: 'btn btn--lg btn--block'
.row
  .col-md-6
    %h2 Pending help requests:
    %ul.supports
      - waiting_for_help.each do |not_done|
        %li= render 'supports/not_done', not_done: not_done
  .col-md-6
    %h2 Recently helped:
    %ul.supports
      - recent_supports.each do |support|
        %li= render support

  .col-md-12.text-center
    = link_to 'See users ranking', users_path, class: 'btn btn-primary'

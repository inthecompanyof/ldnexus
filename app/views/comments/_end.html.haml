.row
  .col-xs-12.event.done
    .image
      = image_tag('i/status-done.png')
    .details
      .who
        %span.bold
          #{support.user.to_s.titleize}
        marked as done
      .when
        = support.finished_at

.row
  .col-md-8.col-md-offset-2
    %h1
      %i.glyphicon.glyphicon-fire
      Need help with #{topic.small_title}?
    %ul.supporters
      - random_supporters.each do |supporter|
        = render 'supporter', supporter: supporter, topic: topic

